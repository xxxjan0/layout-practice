1) сначала не понял, как использовать repeat и fr ,сделал repeat(3, 1fr) и gap 20px
2) долго подбирал auto-fill и minmax, чтобы картинки не разъезжались
3) запомнил grid-template-areas. Сначала запутался в именах областей, потом переписал
4) сложнее всего было расположить 4 карточки статистики и таблицу с уведомлениями, сделал отдельную сетку для stats-area и
 расписал grid-template-areas для дашборда
5) общий контейнер grid, внутри карточек использовал flex-direction: column, чтобы кнопка прилипала к низу